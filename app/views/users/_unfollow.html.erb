<%= form_for(current_user.relationships.find_by(followed_id: user.id),
             html: { method: :delete }, remote: true) do |f| %>
    <div>
      <%= f.hidden_field :followed_id %>
    </div>
    <%= f.submit 'フォロー', class: 'btn btn-large' %>
<% end %>
