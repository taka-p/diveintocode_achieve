<%= form_for(([@project, @project.members.find_by(user_id: user.id)]),
             html: { method: :delete }) do |f| %>
    <% f.hidden_field :project_id %>
    <% f.hidden_field :user_id %>
    <%= f.submit 'プロジェクトメンバーから外す', class: 'btn' %>
<% end %>
